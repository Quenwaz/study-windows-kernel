set(BUILD_SHARED_LIBS TRUE)
BuildTarget(FALSE)
target_compile_definitions(${PROJECT_NAME} PRIVATE -DWIN32 -D_DEBUG -D_WINDOWS -D_USRDLL _DUNICODE -D_UNICODE -DDLL_HIJACKING_EXPORT)

# install(TARGETS ${PROJECT_NAME} LIBRARY DESTINATION ${test_dll_BINARY_DIR})